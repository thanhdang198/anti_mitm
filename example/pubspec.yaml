name: example
description: A new Flutter project.
publish_to: "none"
version: 1.0.0+1

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  anti_mitm:
    path: ../
  flutter:
    sdk: flutter
  http: ^1.1.0
  # anti_mitm: ^0.2.0
  
dev_dependencies:
  very_good_analysis: ^5.1.0

flutter:
  uses-material-design: true
